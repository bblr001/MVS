#pragma once

#include "stdafx.h"

#include <stdexcept>
#include <vector>

#include "CBaslerCameraConfig.h"

using namespace std;

// Constructor allocates the image buffer

CBaslerCameraConfig::CBaslerCameraConfig()
{
	//CBaseCameraConfig::m_SetIndex(atoi(pIndex.c_str());
// 	m_ip = pIP;
// 	m_pixelFormat = pPixelFormat;
// 	m_triggerMode = pTrigM;
// 	m_triggerEdge = pTrigE;
// 	m_exposureTime = atoi(pExpTime.c_str());	
// 	m_debouncerTime = atoi(pDebTime.c_str());	
}


CBaslerCameraConfig::~CBaslerCameraConfig()
{

}

void CBaslerCameraConfig::m_LoadConfigFile(const char * pathConf)
{
// 	try
// 	{
// 	}
// 	catch (CException* e)
// 	{
// 	}
			
}

